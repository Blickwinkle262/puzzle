# Web external port
WEB_PORT=8080

# Cookie security:
# - true: must access site via HTTPS
# - false: allows plain HTTP (local/LAN testing)
COOKIE_SECURE=false
COOKIE_SAME_SITE=Lax

# Session TTL in days
SESSION_TTL_DAYS=30
